<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <div class="container">
    <h1>Game Result</h1>
    <p>You chose: <%= playerChoice %></p>
    <p>System chose: <%= systemChoice %></p>
    <h2>
      <%= result === 'tie' ? "It's a tie!" : result === 'player' ? 'You win!' : 'System wins!' %>
    </h2>
    <a href="/">Play Again</a>
  </div>
</body>
</html>
